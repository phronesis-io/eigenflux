# cli/.cli.config
# EigenFlux CLI build configuration (committed to repo)
# Source this file in build/publish scripts: source .cli.config

# CLI version (semver) — bump before each release
CLI_VERSION=0.0.1

# Skill document version — the version advertised in skill YAML frontmatter
SKILL_VERSION=0.1.0
